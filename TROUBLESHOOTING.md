# Устранение проблем с синхронизацией

## 🔧 Основные проблемы и решения

### 1. **Ревизии показывают NaN или undefined**

**Признаки**:
- В логах видны значения `local: NaN` или `server: undefined`
- Индикатор синхронизации не появляется при изменениях
- Данные не синхронизируются с облаком

**Решение**:
1. Откройте **Настройки** → кнопка **🔢 Сбросить ревизии**
2. Это установит локальную ревизию = 1, серверную = 0
3. Синхронизация должна запуститься автоматически

### 2. **Старый формат данных в облаке**

**Признаки**:
- В логах видно `{"cardsBatches":4}` вместо `{"version":1,"revision":N,"batches":M}`
- Ошибки типа `No cloud data found` при наличии данных
- Размер данных показывается как 0

**Решение**:
1. Откройте **Настройки** → кнопка **🔄 Мигрировать старые данные**
2. Система автоматически конвертирует старый формат в новый
3. После миграции данные должны работать корректно

### 3. **Данные не синхронизируются с облаком**

**Признаки**:
- Красная точка-индикатор не появляется при изменениях
- В настройках локальная и серверная ревизии одинаковые
- При попытке записи в облако ничего не происходит

**Диагностика**:
1. Откройте **Настройки** → посмотрите раздел "Информация о хранилище"
2. Проверьте ревизии: локальная должна быть больше серверной для запуска синхронизации

**Решение**:
1. Попробуйте **🔢 Сбросить ревизии** - установит local=1, server=0
2. Сделайте любое изменение карточки (ответ/редактирование)
3. Должна появиться красная точка и запуститься синхронизация

### 4. **Ошибка "No cloud data found" при наличии данных**

**Причина**: Система пытается читать данные в новом формате, а в облаке старый формат

**Решение**:
1. **🔍 Показать данные из облака** - посмотрите что реально в облаке
2. Если видите старый формат - используйте **🔄 Мигрировать старые данные**
3. Если данных нет совсем - используйте **⬆️ Записать в облако**

### 5. **Полный сброс и начало заново**

Если ничего не помогает:

1. **🗑️ Очистить локальные данные**
2. Перезагрузите страницу
3. Система создаст демо-данные с revision_local = 1
4. Используйте **⬆️ Записать в облако** для первой синхронизации

## 🔍 Диагностические инструменты

### В разделе "Информация о хранилище":
- **Локальные данные** - должны показывать размер данных
- **Ревизия локальная** - текущая версия ваших данных  
- **Ревизия сервера** - версия в облаке
- **Статус синхронизации** - текущее состояние

### В разделе "Действия":
- **🔄 Обновить информацию** - обновить статус без изменений
- **⬇️ Загрузить из облака** - принудительно взять данные с сервера
- **⬆️ Записать в облако** - принудительно отправить локальные данные
- **🔍 Показать данные из облака** - посмотреть что реально в CloudStorage
- **🔄 Мигрировать старые данные** - конвертировать из старого формата
- **🔢 Сбросить ревизии** - исправить проблемы с NaN/undefined
- **🗑️ Очистить локальные данные** - полный сброс

## 📊 Как должна работать синхронизация

### Правильное поведение:
1. **При изменении данных**: локальная ревизия увеличивается на 1
2. **Появление индикатора**: красная пульсирующая точка
3. **Фоновая синхронизация**: данные отправляются в облако
4. **Успешная синхронизация**: точка исчезает, ревизии выравниваются

### Ожидаемые значения ревизий:
- **Первый запуск**: local=1, server=0 (после создания демо-данных)
- **После синхронизации**: local=N, server=N (одинаковые)
- **После изменения**: local=N+1, server=N (локальная больше)
- **После записи в облако**: local=N+1, server=N+1 (снова одинаковые)

## 🚨 Критические ошибки

### "undefined is not an object (evaluating 't.toString')"
- **Причина**: Проблемы с парсингом ревизий
- **Решение**: **🔢 Сбросить ревизии**

### "local: NaN, server: undefined"
- **Причина**: Некорректная инициализация ревизий  
- **Решение**: **🔢 Сбросить ревизии** → **🗑️ Очистить локальные данные** → перезагрузка

### Данные есть, а показывает "No cloud data found"
- **Причина**: Конфликт форматов данных
- **Решение**: **🔄 Мигрировать старые данные**

## 💡 Профилактика

1. **Не редактируйте** карточки одновременно на разных устройствах
2. **Дождитесь синхронизации** (исчезновения красной точки) перед закрытием
3. **Проверяйте статус** в настройках при подозрительном поведении
4. **Используйте стабильное подключение** к интернету

## 🔧 Последовательность действий при проблемах

1. **Обновить информацию** в настройках
2. Если ревизии NaN/undefined → **Сбросить ревизии**
3. Если старый формат в облаке → **Мигрировать старые данные**  
4. Если не помогает → **Очистить локальные данные** → перезагрузка
5. При серьезных проблемах → скопировать логи и обратиться к разработчику