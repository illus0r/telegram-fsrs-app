# Telegram Cloud Storage Architecture v2.0

## Новая система синхронизации с ревизиями

### 🔄 Основные принципы

- **Ревизионная система**: Каждое изменение данных увеличивает номер ревизии
- **Локальный приоритет**: Данные сохраняются сначала локально, затем синхронизируются с облаком
- **Автоматическое разрешение конфликтов**: При конфликте серверная версия всегда побеждает
- **Периодическая синхронизация**: Автоматическая попытка записи каждые 10 секунд
- **Упрощенное хранение**: Данные хранятся в строковом формате, разбитом на батчи

### 📊 Структура данных

#### Локальное хранилище (localStorage)
```
cards_data              # TSV данные карточек (строка)
cards_revision          # Локальная ревизия (число)
cards_server_revision   # Последняя известная ревизия сервера (число)
```

#### Облачное хранилище (Telegram CloudStorage)
```
cards_meta             # Метаданные: {version: 1, revision: N, batches: M}
cards_batch_0          # Первый батч данных
cards_batch_1          # Второй батч данных
...                    # Дополнительные батчи по необходимости
```

### 🔧 Алгоритм работы

#### Инициализация приложения
```
1. Прочитать локальные данные и ревизию
2. Прочитать метаданные из CloudStorage для получения server_revision
3. Определить сценарий:
   
   Если нет локальных И нет серверных данных:
   → Создать демо-данные с revision_local = 1
   
   Если нет локальных ИЛИ revision_local <= revision_server:
   → Загрузить данные из CloudStorage
   → Установить revision_local = revision_server
   
   Иначе:
   → Использовать локальные данные (они новее)
   → Запустить синхронизацию

4. Запустить периодическую синхронизацию каждые 10 секунд
```

#### Сохранение данных локально
```
1. Сохранить данные в localStorage
2. Увеличить revision_local на 1
3. Запустить фоновую синхронизацию с CloudStorage
```

#### Чтение из CloudStorage
```
1. Прочитать метаданные (cards_meta)
2. Прочитать все батчи (cards_batch_0, cards_batch_1, ...)
3. Склеить батчи в одну строку
4. Сохранить локально с revision_local = revision_server
```

#### Запись в CloudStorage
```
1. Проверить актуальность: revision_local > revision_server
2. Прочитать текущую revision_server из метаданных
3. Если revision_server > revision_local (конфликт):
   → Загрузить серверные данные (потеря локальных изменений)
   → Прервать запись
4. Разбить данные на батчи (до 2000 символов каждый)
5. Записать метаданные с новой ревизией
6. Записать все батчи
7. Очистить лишние старые батчи
8. Установить revision_server = revision_local
```

### 🔄 Сценарии синхронизации

#### Первый запуск
- Локально: нет данных
- CloudStorage: нет данных
- **Результат**: Создаются демо-данные с revision_local = 1

#### Данные есть только в облаке
- Локально: нет данных
- CloudStorage: revision_server = 5
- **Результат**: Загружаются данные из облака, revision_local = 5

#### Локальные данные новее
- Локально: revision_local = 3
- CloudStorage: revision_server = 2
- **Результат**: Используются локальные данные, запускается синхронизация

#### Серверные данные новее
- Локально: revision_local = 2  
- CloudStorage: revision_server = 5
- **Результат**: Загружаются серверные данные, revision_local = 5

#### Конфликт при записи
- Попытка записи с revision_local = 3
- Обнаружено: revision_server = 4
- **Результат**: Загружаются серверные данные, локальные изменения теряются

### 🏗️ Техническая реализация

#### Основные функции
```typescript
// Инициализация системы хранения
initializeStorage(): Promise<string | null>

// Сохранение данных локально с увеличением ревизии
saveDataLocally(data: string, incrementRevision = true): Promise<void>

// Попытка чтения из CloudStorage
tryReadFromCloud(): Promise<string | null>

// Попытка записи в CloudStorage
tryWriteToCloud(): Promise<boolean>

// Запуск/остановка периодической синхронизации
startPeriodicSync(): void
stopPeriodicSync(): void
```

#### Управление состоянием синхронизации
```typescript
interface SyncStatus {
  hasUnsavedChanges: boolean    // revision_local > revision_server
  isSyncing: boolean           // Идет процесс синхронизации
  revisionLocal: number        // Локальная ревизия
  revisionServer: number       // Серверная ревизия
  lastSyncAttempt?: Date      // Время последней попытки
  lastSyncError?: string      // Текст последней ошибки
}
```

### ⚡ Оптимизации

#### Разбиение на батчи
- Максимальный размер батча: 2000 символов
- Батчи нумеруются: cards_batch_0, cards_batch_1, ...
- При записи количество батчей может изменяться

#### Предотвращение спама
- Минимальный интервал между попытками синхронизации: 5 секунд
- Периодическая синхронизация: каждые 10 секунд
- Синхронизация только при наличии несохраненных изменений

#### Обработка ошибок
- При ошибке чтения из CloudStorage используются локальные данные
- При ошибке записи повторные попытки каждые 10 секунд
- Все операции логируются для диагностики

### 🔍 Диагностика и отладка

#### Индикатор синхронизации
- 🔄 Синий пульсирующий - идет синхронизация
- ⚠️ Красный - есть несохраненные изменения
- Скрыт - все синхронизировано

#### Панель настроек
- Информация о ревизиях
- Принудительная загрузка из облака
- Принудительная запись в облако
- Очистка локальных данных
- Просмотр логов отладки

#### Полезная информация
```javascript
// Получить информацию о локальном хранилище
getLocalStorageInfo(): {
  hasData: boolean,
  size: number,
  revision: number,
  serverRevision: number
}

// Очистить все локальные данные
clearLocalStorage(): void
```

### 🎯 Преимущества новой системы

#### Надежность
- Данные всегда сохраняются локально первыми
- Автоматическое восстановление после сбоев
- Четкие правила разрешения конфликтов

#### Производительность
- Мгновенное сохранение в UI
- Фоновая синхронизация без блокировки
- Минимальное количество сетевых запросов

#### Простота
- Упрощенная структура метаданных
- Прозрачная система ревизий
- Понятное состояние синхронизации

#### Масштабируемость  
- Динамическое разбиение на батчи
- Эффективная очистка старых данных
- Готовность к будущим расширениям

### 🔄 Миграция с предыдущей версии

Новая система автоматически обнаруживает старые данные и мигрирует их:
1. Обнаруживает данные в старом формате
2. Загружает их в новую структуру
3. Устанавливает начальные ревизии
4. Начинает работу по новой схеме

Старые данные сохраняются до успешной миграции.