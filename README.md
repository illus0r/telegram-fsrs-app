# Telegram Anki FSRS

Минималистичное приложение для изучения карточек с алгоритмом FSRS в Telegram Mini App.

## Особенности

- 📚 **Интервальное повторение** с алгоритмом FSRS (последняя стабильная версия ts-fsrs)
- 💾 **Облачное хранение** через Telegram CloudStorage API
- 📝 **TSV формат** для редактирования карточек со всеми метаданными FSRS
- 📱 **Telegram UI** с нативными кнопками (MainButton, BackButton)
- ⚡ **Минималистичный дизайн** следует Telegram UI guidelines
- 💾 **Автосохранение прогресса** при каждом ответе на карточку
- ⏱️ **Показ интервалов** времени до следующего повторения на кнопках

## Технологии

- **Frontend**: React + TypeScript + Vite
- **FSRS**: ts-fsrs (последняя стабильная версия)
- **Storage**: Telegram CloudStorage API (fallback: localStorage)
- **Deploy**: GitHub Pages с автоматическим деплоем + ручной `pnpm deploy`

## Структура проекта

```
src/
├── components/
│   ├── StudyView.tsx      # Повторение карточек
│   └── EditView.tsx       # Редактирование TSV
├── lib/
│   ├── telegram.ts        # Telegram WebApp API
│   ├── storage.ts         # CloudStorage обёртка
│   └── fsrs.ts           # FSRS логика + TSV парсер
├── App.tsx               # Роутинг между экранами
└── index.tsx             # Точка входа
```

## Формат TSV

```tsv
question	answer	due	stability	difficulty	elapsed_days	scheduled_days	reps	lapses	state	last_review
Hello	Привет	2024-01-01T00:00:00.000Z	2.5	3.1	0	1	1	0	2	2024-01-01T00:00:00.000Z
World	Мир					0	0	0	0	0	
Cat	Кот					0	0	0	0	0	
```

- Пустые метаданные FSRS = новая карточка
- Все поля необходимые для работы библиотеки ts-fsrs
- Демо-карты включены по умолчанию

## Разработка и деплой

### Установка
```bash
npm install
npm run dev
```

### Деплой
```bash
# Ручной деплой
npm run deploy

# Автоматический при пуше в main
git push origin main
```

### Требования
- Node.js 16+ (протестировано на 16.11.1)
- npm 8+

## Использование

### Изучение карточек
1. Нажмите на карточку → увидите ответ
2. Выберите оценку по времени: **1 мин**, **10 мин**, **2 ч**, **11 д**
3. FSRS планирует следующее повторение и автоматически сохраняет прогресс
4. На кнопках показано время до следующего повторения

### Редактирование
**Четыре способа управления карточками:**

1. **Создание новой карточки:**
   - Синяя кнопка (+) справа в верхнем меню
   - Создает карточку с шаблонным текстом
   - Открывает экран редактирования

2. **Редактирование отдельной карточки:**
   - Кнопка "Редактировать" под карточкой
   - Поля "Вопрос" и "Ответ" (ответ может быть пустым)
   - Поддержка многострочности
   - Кнопка удаления карточки
   - Прогресс изучения сохраняется

3. **Массовое редактирование TSV:**
   - Кнопка с иконкой таблицы (≡) справа в верхнем меню
   - Текстовое поле с TSV → **"Сохранить"**
   - **BackButton** для отмены
   - Используйте `==` вместо табуляций
   - Поддержка фронтальных карт (без ответа)

4. **Удаление карточки:**
   - Кнопка "🗑️ Удалить карточку" в режиме редактирования
   - Подтверждение удаления
   - Прогресс других карточек сохраняется

### Многострочность и форматирование
- Поддерживается в вопросах и ответах
- В TSV используйте `\n` для переносов строк
- Используйте `==` вместо табуляций в TSV для удобства
- Пример: `What is React?==React is a JavaScript\nlibrary for building\nuser interfaces`
- Фронтальные карты: `Question without answer==`

## Хранение данных

- **CloudStorage ключ**: `cards` (все карты в одном TSV файле)
- **Ограничения**: 1024 ключа на пользователя в Telegram
- **Fallback**: localStorage при разработке в браузере  
- **Совместимость**: Telegram WebApp 6.1+ для CloudStorage
- **Многострочность**: Корректное экранирование переносов в TSV формате
- **TSV удобство**: Замена `==` на табуляции при сохранении
- **Фронтальные карты**: Поддержка карт без ответа

## Статус

✅ **Готово к использованию**
- Приложение собрано и задеплоено
- GitHub Pages настроен
- Telegram WebApp API интегрирован
- FSRS алгоритм работает с автосохранением прогресса
- CloudStorage с fallback на localStorage
- Демо-карты включены с примерами
- Кнопки показывают интервалы повторений
- Интерфейс оптимизирован для Telegram
- Поддержка фронтальных карт и удаления
- Удобный TSV редактор с заменой `==` на табуляции

### Следующие шаги
1. Настроить бота через @BotFather
2. Добавить Mini App: `/setminiapp`
3. URL: `https://fsrs.dianov.org/`

## Лицензия

MIT License