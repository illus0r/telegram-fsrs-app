# Отладка проблемы кнопки "Сохранить"

## Описание проблемы
Кнопка "Сохранить" в режиме редактирования может не работать из-за проблем с Telegram WebApp MainButton API.

## Исправления в версии v1.1

### 1. Исправлена логика callback-ов Telegram MainButton
- Добавлено удаление предыдущих callback-ов перед установкой новых
- Используется `offClick()` для очистки старых обработчиков
- Предотвращена ситуация с множественными callback-ами

### 2. Улучшена логика активации кнопки
- Кнопка активируется только при наличии изменений И отсутствии ошибок валидации
- Добавлена задержка для синхронизации состояния
- Улучшено логирование для отладки

### 3. Добавлена отладочная кнопка
- В режиме редактирования появляется дополнительная кнопка "💾 Сохранить (отладка)"
- Работает независимо от Telegram MainButton
- Позволяет сохранить изменения даже если MainButton не отвечает

## Как тестировать

### В Telegram Mini App:
1. Откройте приложение в Telegram
2. Нажмите кнопку "Редактировать" внизу экрана
3. Внесите изменения в текст карточек
4. Проверьте что кнопка "Сохранить" вверху стала активной (синий цвет)
5. Нажмите "Сохранить" - должен произойти переход к экрану изучения

### В браузере:
1. Откройте https://fsrs.dianov.org/ в браузере
2. Должна появиться отладочная информация в консоли
3. Кнопка Telegram не работает, но должна появиться зеленая кнопка "💾 Сохранить (отладка)"

### Отладка через DevTools:
1. Откройте DevTools (F12)
2. Перейдите во вкладку Console
3. Ищите сообщения начинающиеся с "EditView:"
4. Проверьте последовательность:
   ```
   EditView: Loading current TSV data: xxx characters
   EditView: Setting up buttons
   EditView: TSV change detected
   EditView: Enabling/Disabling save button after change
   EditView: handleSave called
   ```

## Возможные проблемы и решения

### Проблема: Кнопка не активируется при изменениях
**Причина**: Ошибки валидации TSV или проблемы с определением изменений  
**Решение**: Проверьте формат TSV, используйте отладочную кнопку

### Проблема: Кнопка активна, но не сохраняет
**Причина**: Проблемы с Telegram WebApp API или CloudStorage  
**Решение**: Используйте отладочную кнопку, проверьте логи в консоли

### Проблема: Данные не сохраняются после нажатия
**Причина**: Ошибки при парсинге TSV или проблемы с storage  
**Решение**: Проверьте консоль на наличие ошибок, убедитесь в корректности формата данных

## Формат TSV для проверки
```
question	answer	due	stability	difficulty	elapsed_days	scheduled_days	reps	lapses	state	last_review
Hello	Привет	2024-01-01T00:00:00.000Z	0	0	0	0	0	0	0	
Test	Тест	2024-01-01T00:00:00.000Z	0	0	0	0	0	0	0	
```

## Логи для анализа

### Нормальный сценарий:
```
EditView: Loading current TSV data: 245 characters
EditView: Setting up buttons
EditView: TSV change detected {newLength: 250, originalLength: 245, hasChanged: true}
EditView: Enabling save button after change
EditView: handleSave called {hasChanges: true, error: null, tsvDataLength: 250}
EditView: Calling onSave with TSV data...
App: Saving TSV data...
App: Data saved successfully
```

### Проблемный сценарий:
```
EditView: handleSave called {hasChanges: false, error: null}
EditView: Save blocked: {hasChanges: false, error: null}
```

## Контакты
Если проблема не решена, проверьте:
1. Версию Telegram (нужна 6.1+ для CloudStorage)
2. Работу JavaScript в браузере
3. Наличие ошибок в DevTools Console
4. Используйте отладочную кнопку как временное решение